{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build and Test",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/unit_tests",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Full Build and Test",
      "stopOnEntry": false,
      "console": "integratedTerminal"
    },
    {
      "name": "Attach to Process by PID",
      "type": "lldb",
      "request": "attach",
      "pid": "${command:pickProcess}"
    }
  ]
}